# rust

## How to install in Linux

```
curl --proto '=https' --tlsv1.2 https://sh.rustup.rs -sSf | sh
```

## Verify install
```
rustc --version
```

## Cargo
```
cargo new ${PROJECT}
cargo build
cargo build --release
cargo run
cargo check

```